using System;
using System.Text.Json;

class Program {
   static void Main() {
      // example 1
      string[] a = {"May", "June"};
      var s1 = JsonSerializer.Serialize(a);
      // example 2
      // print
      Console.WriteLine(s);
   }
}
