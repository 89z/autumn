use std::{
   thread,
   time::Duration,
   time::Instant
};

fn main() {
   let now_o = Instant::now();
   let dur_o = Duration::from_secs(5);
   loop {
      thread::sleep(o);
      let duration = start.elapsed();
   }
}
